{
  "name": "etl_scripts",
  "version": "1.0.0",
  "description": "Transform raw data to db",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc && node dist/etl_scripts/src/migration_raw_to_db/migration_to_db.js"
  },
  "keywords": [
    "ETL"
  ],
  "author": "vibatious",
  "license": "ISC",
  "dependencies": {
    "mysql2": "^2.2.5"
  },
  "devDependencies": {
    "@types/node": "^16.4.2"
  }
}
